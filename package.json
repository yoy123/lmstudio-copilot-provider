{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "lmstudio-copilot-provider",
  "displayName": "LM Studio for Copilot Chat",
  "description": "Use local LM Studio models inside VS Code Copilot Chat with streaming, tool calling, and optional image generation.",
  "version": "1.30.6",
  "publisher": "DanLambiase",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/yoy123/lmstudio-copilot-provider.git"
  },
  "bugs": {
    "url": "https://github.com/yoy123/lmstudio-copilot-provider/issues"
  },
  "homepage": "https://github.com/yoy123/lmstudio-copilot-provider#readme",
  "icon": "assets/Latest.png",
  "engines": {
    "vscode": "^1.104.0"
  },
  "categories": [
    "AI",
    "Chat"
  ],
  "keywords": [
    "lm studio",
    "copilot",
    "chat",
    "language model",
    "ai",
    "llm"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "configuration": [
      {
        "id": "lmstudio-copilot",
        "title": "LM Studio",
        "order": 1,
        "properties": {
          "lmstudio-copilot.logLevel": {
            "type": "string",
            "enum": [
              "verbose",
              "info",
              "warning",
              "error",
              "none"
            ],
            "enumDescriptions": [
              "All messages including detailed diagnostics",
              "State changes, model discovery, and important events",
              "Warnings and errors only",
              "Errors only",
              "No Output Channel logging"
            ],
            "default": "verbose",
            "order": 0,
            "description": "Controls how much diagnostic information is written to the LM Studio output channel"
          },
          "lmstudio-copilot.serverUrl": {
            "type": "string",
            "default": "http://localhost:1234",
            "order": 1,
            "description": "LM Studio server URL"
          },
          "lmstudio-copilot.apiKey": {
            "type": "string",
            "default": "",
            "scope": "machine",
            "ignoreSync": true,
            "order": 2,
            "markdownDescription": "API key for LM Studio server authentication. This is used for chat completions against the LM Studio server and is separate from #lmstudio-copilot.imageGenApiKey#."
          },
          "lmstudio-copilot.autoRefreshModels": {
            "type": "boolean",
            "default": true,
            "order": 3,
            "description": "Automatically refresh available models on startup using both the LM Studio CLI and the local server"
          },
          "lmstudio-copilot.requestTimeout": {
            "type": "number",
            "default": 60000,
            "order": 4,
            "description": "Request timeout in milliseconds"
          },
          "lmstudio-copilot.cliPath": {
            "type": "string",
            "default": "lms",
            "order": 5,
            "description": "Path to the LM Studio CLI. The extension uses this to discover installed models, auto-start the server, and lazy-load models on first use."
          },
          "lmstudio-copilot.launchCommand": {
            "type": "string",
            "default": "",
            "order": 6,
            "description": "Fallback command to launch LM Studio in a VS Code terminal when the LM Studio CLI is unavailable"
          },
          "lmstudio-copilot.terminalName": {
            "type": "string",
            "default": "LM Studio Server",
            "order": 7,
            "description": "Name of the integrated terminal used only when launchCommand fallback is needed"
          },
          "lmstudio-copilot.autoStartServer": {
            "type": "boolean",
            "default": true,
            "order": 8,
            "description": "Automatically ensure the LM Studio daemon and local server are running when the extension starts"
          },
          "lmstudio-copilot.startupWaitMs": {
            "type": "number",
            "default": 3000,
            "order": 9,
            "description": "Milliseconds to wait before checking LM Studio connection after server start"
          },
          "lmstudio-copilot.treatAsRemote": {
            "type": "boolean",
            "default": false,
            "order": 10,
            "markdownDescription": "Always treat the configured server as a **remote** server — disables all local CLI operations (auto-start, model discovery via `lms`, server stop). Enable this when you tunnel a remote LM Studio over SSH so its URL looks like `localhost` but should not trigger local CLI commands."
          },
          "lmstudio-copilot.autoFixByokUtilityModel": {
            "type": "boolean",
            "default": true,
            "order": 11,
            "description": "Automatically configure VS Code chat utility model defaults for BYOK compatibility when utility settings are not explicitly set. Prevents 'copilot-utility-small' configuration errors."
          },
          "lmstudio-copilot.allowStartupTasksOnRemote": {
            "type": "boolean",
            "default": false,
            "order": 12,
            "description": "Allow auto-start and auto-refresh startup tasks when this extension is running in a remote extension host (for example Remote SSH). Disabled by default to avoid remote startup slowdowns/timeouts."
          }
        }
      },
      {
        "id": "lmstudio-copilot.chat",
        "title": "Chat and Tools",
        "order": 2,
        "properties": {
          "lmstudio-copilot.enableTerminalTool": {
            "type": "boolean",
            "default": true,
            "order": 1,
            "description": "Allow LM Studio model tool calls to run commands in VS Code integrated terminal"
          },
          "lmstudio-copilot.terminalToolName": {
            "type": "string",
            "default": "LM Studio Tool Terminal",
            "order": 2,
            "description": "Integrated terminal name used by model tool commands"
          },
          "lmstudio-copilot.terminalToolTimeout": {
            "type": "number",
            "default": 30000,
            "order": 3,
            "description": "Timeout in milliseconds for model-invoked terminal commands"
          },
          "lmstudio-copilot.maxInputTokens": {
            "type": "number",
            "default": 131072,
            "order": 4,
            "description": "Maximum input tokens (context window size). Default is 128k."
          },
          "lmstudio-copilot.maxOutputTokens": {
            "type": "number",
            "default": 16384,
            "order": 5,
            "description": "Maximum output tokens. Default is 16k."
          },
          "lmstudio-copilot.maxTools": {
            "type": "number",
            "default": 20,
            "order": 6,
            "description": "Maximum number of tools to send to the model per request. Local models struggle with many tools (86+ is common with MCP servers). Tools used in the current conversation are always included; the rest are prioritized by name simplicity. Set to 0 for no limit."
          },
          "lmstudio-copilot.enableToolCalling": {
            "type": "boolean",
            "default": true,
            "order": 7,
            "description": "Enable tool calling support for models that support it (e.g., Qwen, Llama 3.1+, Mistral)."
          },
          "lmstudio-copilot.injectSystemPrompt": {
            "type": "boolean",
            "default": true,
            "order": 8,
            "description": "Prepend a system message with coding-assistant instructions. Disable if your LM Studio chat template already includes system instructions."
          },
          "lmstudio-copilot.enableThinking": {
            "type": "boolean",
            "default": true,
            "order": 9,
            "description": "Allow models to use extended thinking / chain-of-thought (enable_thinking parameter). Disable for models that produce noisy <think> blocks or error on the parameter."
          },
          "lmstudio-copilot.reasoningEffort": {
            "type": "string",
            "enum": [
              "default",
              "none",
              "low",
              "medium",
              "high"
            ],
            "enumDescriptions": [
              "Do not send a reasoning_effort parameter — the model uses its built-in default.",
              "Minimal reasoning trace. Faster responses, lower token usage.",
              "Balanced reasoning. Good for most tasks.",
              "Maximum reasoning depth. Best for complex problems, uses more tokens."
            ],
            "default": "default",
            "order": 10,
            "description": "Reasoning effort level sent to models that support the reasoning_effort parameter (e.g. o1, o3, QwQ). Has no effect on models that do not support the parameter."
          },
          "lmstudio-copilot.gemmaChannelThinkingTransform": {
            "type": "boolean",
            "default": true,
            "order": 11,
            "description": "For Gemma-family models, transform channel-style thinking output so hidden reasoning is not shown in chat responses."
          },
          "lmstudio-copilot.enableInlineCompletions": {
            "type": "boolean",
            "default": false,
            "order": 12,
            "description": "Enable experimental LM Studio-powered inline code completions (ghost text)."
          },
          "lmstudio-copilot.inlineCompletionModel": {
            "type": "string",
            "default": "",
            "order": 13,
            "description": "Model ID to use for inline completions. Leave blank to auto-pick the first available non-embedding model."
          },
          "lmstudio-copilot.inlineCompletionMinPrefixChars": {
            "type": "number",
            "default": 3,
            "order": 14,
            "description": "Minimum prefix length before automatic inline completion requests are made."
          },
          "lmstudio-copilot.inlineCompletionMaxPrefixChars": {
            "type": "number",
            "default": 6000,
            "order": 15,
            "description": "Maximum number of characters before the cursor sent as completion context."
          },
          "lmstudio-copilot.inlineCompletionMaxSuffixChars": {
            "type": "number",
            "default": 1200,
            "order": 16,
            "description": "Maximum number of characters after the cursor sent as completion context."
          },
          "lmstudio-copilot.inlineCompletionMaxTokens": {
            "type": "number",
            "default": 96,
            "order": 17,
            "description": "Maximum output tokens for each inline completion request."
          },
          "lmstudio-copilot.inlineCompletionMaxChars": {
            "type": "number",
            "default": 512,
            "order": 18,
            "description": "Maximum characters returned as a single inline completion suggestion."
          },
          "lmstudio-copilot.inlineCompletionTimeoutMs": {
            "type": "number",
            "default": 8000,
            "order": 19,
            "description": "Timeout in milliseconds for each inline completion request."
          },
          "lmstudio-copilot.inlineCompletionTemperature": {
            "type": "number",
            "default": 0.2,
            "order": 20,
            "description": "Sampling temperature for inline completions. Lower values are more deterministic."
          }
        }
      },
      {
        "id": "lmstudio-copilot.image",
        "title": "Image Generation",
        "order": 3,
        "properties": {
          "lmstudio-copilot.imageGenBackend": {
            "type": "string",
            "enum": [
              "dalle",
              "a1111"
            ],
            "default": "dalle",
            "order": 1,
            "description": "Image generation backend. 'dalle' = OpenAI-compatible image API. 'a1111' = Automatic1111 / Stable Diffusion Web UI."
          },
          "lmstudio-copilot.imageGenEndpointUrl": {
            "type": "string",
            "default": "",
            "order": 2,
            "description": "Base URL for the image generation server. LM Studio does NOT have an image generation endpoint — leave blank to see setup instructions. For DALL-E (OpenAI): https://api.openai.com. For A1111/Stable Diffusion: typically http://localhost:7860."
          },
          "lmstudio-copilot.imageGenApiKey": {
            "type": "string",
            "default": "",
            "scope": "machine",
            "ignoreSync": true,
            "order": 3,
            "markdownDescription": "API key for the image generation backend. Use this for OpenAI DALL-E or another OpenAI-compatible image API. This key is separate from #lmstudio-copilot.apiKey# used for LM Studio chat completions."
          },
          "lmstudio-copilot.imageGenModel": {
            "type": "string",
            "default": "",
            "order": 4,
            "description": "Image model to use for the OpenAI-compatible image backend. Leave blank to use 'dall-e-3' for OpenAI, or auto-detect/omit the model for other compatible servers. Deprecated values like dall-e-2 are normalized to dall-e-3."
          },
          "lmstudio-copilot.imageGenWidth": {
            "type": "number",
            "default": 1024,
            "order": 5,
            "description": "Default image width in pixels."
          },
          "lmstudio-copilot.imageGenHeight": {
            "type": "number",
            "default": 1024,
            "order": 6,
            "description": "Default image height in pixels."
          },
          "lmstudio-copilot.imageGenSteps": {
            "type": "number",
            "default": 20,
            "order": 7,
            "description": "Default number of diffusion steps (A1111 backend only)."
          },
          "lmstudio-copilot.imageGenOutputDir": {
            "type": "string",
            "default": "",
            "order": 8,
            "description": "Directory to save generated images. Workspace-relative or absolute. Defaults to 'generated-images/' inside the workspace."
          }
        }
      }
    ],
    "commands": [
      {
        "command": "lmstudio-copilot.refreshModels",
        "title": "LM Studio: Refresh Available Models"
      },
      {
        "command": "lmstudio-copilot.startServer",
        "title": "LM Studio: Start Server in Integrated Terminal"
      },
      {
        "command": "lmstudio-copilot.stopServer",
        "title": "LM Studio: Stop Server Terminal"
      },
      {
        "command": "lmstudio-copilot.checkConnection",
        "title": "LM Studio: Check Server Connection"
      }
    ],
    "languageModelChatProviders": [
      {
        "vendor": "lmstudio",
        "displayName": "LM Studio"
      }
    ],
    "languageModelTools": [
      {
        "name": "lmstudio_run_in_terminal",
        "toolReferenceName": "runInTerminal",
        "displayName": "Run In Terminal",
        "modelDescription": "Executes a shell command in VS Code integrated terminal and returns its full stdout, stderr, and exit code. Use for build, test, install, lint, and diagnostics commands.",
        "userDescription": "Runs a shell command and returns the output to the model.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "terminal",
          "shell",
          "command"
        ],
        "inputSchema": {
          "type": "object",
          "properties": {
            "command": {
              "type": "string",
              "description": "Shell command to execute"
            },
            "cwd": {
              "type": "string",
              "description": "Working directory (absolute or workspace-relative). Defaults to workspace root."
            }
          },
          "required": [
            "command"
          ]
        }
      },
      {
        "name": "lmstudio_read_file",
        "toolReferenceName": "readFile",
        "displayName": "Read File",
        "modelDescription": "Reads the contents of a file from disk and returns them with line numbers. Accepts workspace-relative or absolute paths. Optionally specify a line range.",
        "userDescription": "Reads a file and returns its contents to the model.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "file",
          "read",
          "filesystem"
        ],
        "inputSchema": {
          "type": "object",
          "properties": {
            "path": {
              "type": "string",
              "description": "File path (absolute or workspace-relative)"
            },
            "startLine": {
              "type": "number",
              "description": "First line to read (1-based, inclusive). Omit for beginning of file."
            },
            "endLine": {
              "type": "number",
              "description": "Last line to read (1-based, inclusive). Omit for end of file."
            }
          },
          "required": [
            "path"
          ]
        }
      },
      {
        "name": "lmstudio_write_file",
        "toolReferenceName": "writeFile",
        "displayName": "Write File",
        "modelDescription": "Writes or overwrites a file on disk with the provided content. Creates parent directories if needed. Use when you want to create or modify a file.",
        "userDescription": "Writes content to a file on disk.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "file",
          "write",
          "filesystem",
          "edit"
        ],
        "inputSchema": {
          "type": "object",
          "properties": {
            "path": {
              "type": "string",
              "description": "File path (absolute or workspace-relative)"
            },
            "content": {
              "type": "string",
              "description": "Full content to write to the file"
            },
            "createDirectories": {
              "type": "boolean",
              "description": "Create parent directories if they don't exist. Defaults to true."
            }
          },
          "required": [
            "path",
            "content"
          ]
        }
      },
      {
        "name": "lmstudio_list_directory",
        "toolReferenceName": "listDirectory",
        "displayName": "List Directory",
        "modelDescription": "Lists the contents of a directory. Returns files and folders with icons. Can recurse into subdirectories up to a configurable depth. Skips node_modules, .git, dist etc.",
        "userDescription": "Lists files and folders in a directory.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "file",
          "directory",
          "filesystem",
          "ls"
        ],
        "inputSchema": {
          "type": "object",
          "properties": {
            "path": {
              "type": "string",
              "description": "Directory path (absolute or workspace-relative). Defaults to workspace root."
            },
            "recursive": {
              "type": "boolean",
              "description": "Recurse into subdirectories. Defaults to false."
            },
            "maxDepth": {
              "type": "number",
              "description": "Maximum recursion depth when recursive is true. Defaults to 4."
            }
          }
        }
      },
      {
        "name": "lmstudio_search_files",
        "toolReferenceName": "searchFiles",
        "displayName": "Search Files",
        "modelDescription": "Searches for a text pattern or regex across all files in the workspace. Returns matching lines with file path, line number, and surrounding context. Uses ripgrep when available for speed.",
        "userDescription": "Searches for text or patterns across workspace files.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "search",
          "grep",
          "filesystem",
          "find"
        ],
        "inputSchema": {
          "type": "object",
          "properties": {
            "pattern": {
              "type": "string",
              "description": "Text or regex pattern to search for"
            },
            "glob": {
              "type": "string",
              "description": "Glob pattern to restrict which files are searched (e.g. '**/*.ts')"
            },
            "isRegex": {
              "type": "boolean",
              "description": "Treat pattern as a regular expression. Defaults to false."
            },
            "caseSensitive": {
              "type": "boolean",
              "description": "Case-sensitive matching. Defaults to false."
            },
            "maxResults": {
              "type": "number",
              "description": "Maximum number of matches to return. Defaults to 50."
            },
            "contextLines": {
              "type": "number",
              "description": "Number of context lines to show around each match. Defaults to 2."
            }
          },
          "required": [
            "pattern"
          ]
        }
      },
      {
        "name": "lmstudio_get_codebase_index",
        "toolReferenceName": "getCodebaseIndex",
        "displayName": "Get Codebase Index",
        "modelDescription": "Walks the entire workspace and returns a structured index of all source files with their paths, sizes, and exported/declared symbols (functions, classes, constants, etc.). Use this to understand the overall codebase layout before reading individual files, or when the user asks about the project structure.",
        "userDescription": "Builds a structural index of the workspace, listing every file and its top-level declarations.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "codebase",
          "index",
          "symbols",
          "workspace",
          "structure"
        ],
        "inputSchema": {
          "type": "object",
          "properties": {
            "includeSymbols": {
              "type": "boolean",
              "description": "Extract top-level symbol names (exported functions, classes, constants, etc.) from each code file. Defaults to true."
            },
            "maxFileSizeKB": {
              "type": "number",
              "description": "Maximum file size in KB to process for symbol extraction. Files larger than this limit are still listed but their symbols are not extracted. Defaults to 128, capped at 512."
            }
          }
        }
      },
      {
        "name": "lmstudio_generate_image",
        "toolReferenceName": "generateImage",
        "displayName": "Generate Image",
        "modelDescription": "Generates an image from a text prompt using a configured image generation backend (DALL-E compatible API or Automatic1111 Stable Diffusion). Saves the image to disk and displays it inline in chat. Supports optional negative prompt, diffusion steps, and output path.",
        "userDescription": "Generates an image from a text description.",
        "canBeReferencedInPrompt": true,
        "tags": [
          "image",
          "generate",
          "dalle",
          "stable-diffusion",
          "ai"
        ],
        "inputSchema": {
          "type": "object",
          "properties": {
            "prompt": {
              "type": "string",
              "description": "Text description of the image to generate"
            },
            "negativePrompt": {
              "type": "string",
              "description": "Things to exclude from the image (A1111 backend only)"
            },
            "steps": {
              "type": "number",
              "description": "Diffusion steps (A1111 backend only). Defaults to lmstudio-copilot.imageGenSteps setting."
            },
            "savePath": {
              "type": "string",
              "description": "Workspace-relative or absolute path to save the image. Defaults to generated-images/<timestamp>.png."
            }
          },
          "required": [
            "prompt"
          ]
        }
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && node esbuild.js",
    "check-types": "tsc --noEmit",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && node esbuild.js --production",
    "package:vsix": "vsce package",
    "publish:vsce": "vsce publish",
    "lint": "eslint src",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.104.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "@vscode/vsce": "^3.7.1",
    "esbuild": "^0.20.0",
    "eslint": "^8.56.0",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.3.0"
  }
}
